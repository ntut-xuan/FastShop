Get the count of the item with the specific ID.
---
tags:
  - item

description: Get the count of the item with the specific ID.

parameters:
  - in: path
    name: ID
    description: "The ID of the item which you want to query. \n\n `4XX` if operation is fail. (See Response section below)"
    schema:
      type: string
    required: true

responses:
  200:
    description: OK
    content:
      application/json:
        schema:
          type: object
          properties:
            count:
              type: integer
              example: 55
  403:
    description: The item query by the ID is absent.
    content:
      application/json:
        schema:
          $ref: "#/definitions/message"
        example:
          message: The specific item is absent.
